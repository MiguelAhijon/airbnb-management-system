<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmación de Reserva | HomyGo</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to right, #ff6ec7 0%, #fff 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 40px 50px;
            max-width: 600px;
            text-align: center;
        }
        .check-icon {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            border: 4px solid #4CAF50;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            animation: pulse 1s ease-in-out infinite;
        }
        .check-icon svg {
            width: 45px;
            height: 45px;
            stroke: #4CAF50;
            stroke-width: 4;
            fill: none;
            stroke-linecap: round;
            stroke-linejoin: round;
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            animation: draw 1s forwards ease-in-out;
        }
        @keyframes draw {
            to { stroke-dashoffset: 0; }
        }
        @keyframes pulse {
            0%,100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        h2 {
            color: #333;
            margin-bottom: 10px;
        }
        p {
            color: #555;
            font-size: 16px;
            margin: 6px 0;
        }
        .resumen {
            text-align: left;
            margin-top: 20px;
            background: #f9f9f9;
            padding: 15px;
            border-radius: 10px;
        }
        .resumen p { margin: 5px 0; }
        .btn {
            display: inline-block;
            margin-top: 20px;
            background-color: #ff6ec7;
            color: white;
            text-decoration: none;
            padding: 12px 25px;
            border-radius: 6px;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .btn:hover { background-color: #ff3385; }
    </style>
</head>
<body>

<div class="container">
    <div class="check-icon">
        <svg viewBox="0 0 52 52">
            <path d="M14 27l7 7 17-17"/>
        </svg>
    </div>

    <h2>¡Tu viaje está confirmado, <span th:text="${reserva?.inquilino?.nombre ?: 'Miguel'}"></span>!</h2>
    <p>Gracias por reservar con <strong>HomyGo</strong> 💗</p>

    <div class="resumen" th:if="${reserva != null}">
        <p><strong>📍 Dirección:</strong> <span th:text="${reserva.inmueble?.direccion}"></span></p>
        <p><strong>📅 Fechas:</strong>
            <span th:text="${#dates.format(reserva.fechaInicio, 'dd/MM/yyyy')}"></span> →
            <span th:text="${#dates.format(reserva.fechaFin, 'dd/MM/yyyy')}"></span>
        </p>
        <p><strong>🌙 Noches:</strong>
            <span th:text="${T(java.time.temporal.ChronoUnit).DAYS.between(reserva.fechaInicio.toInstant().atZone(T(java.time.ZoneId).systemDefault()).toLocalDate(),
                     reserva.fechaFin.toInstant().atZone(T(java.time.ZoneId).systemDefault()).toLocalDate())}"></span>
        </p>
        <p><strong>💰 Total:</strong>
            <span th:text="${#numbers.formatDecimal(reserva.precioTotal, 2, 2)} + ' €'"></span>
        </p>
    </div>

    <a th:href="@{/perfil/mis_reservas}" class="btn">Ver mis reservas</a>
</div>

</body>
</html>
